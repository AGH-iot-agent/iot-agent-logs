server:
  port: 8080

spring:
  application:
    name: iot-agent-logs

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always
